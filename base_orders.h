#include "order.h"
#include <vector>

/*
class TBaseOrders {
public:
    TBaseOrders();

    void AddOrders(const std::vector<TOrder>& newOrders);

    std::vector<TOrder> GetOrders() const;

    void Clear();

private:
    std::vector<TOrder> Orders;
};*/